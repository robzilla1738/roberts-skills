---
description: Design or implement a macOS Dynamic Island / notch-style app (NSPanel, modules)
---

Notch-style macOS task: **$ARGUMENTS**

Load and follow the **macos-notch** skill at `${CLAUDE_PLUGIN_ROOT}/skills/macos-notch/SKILL.md`:
read the hub, then the foundations and the module spokes the task needs (media, HUD, shelf,
peek, calendar, camera, agent monitor) plus integration/shipping. Use hardware-aligned
top-center NSPanel geometry and a clean state machine.

If $ARGUMENTS is empty, ask which notch modules and behaviors are wanted.
